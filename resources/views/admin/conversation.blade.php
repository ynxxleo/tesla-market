@extends('layouts.app')@section('title','Conversation #'.$conversation->id.' — Admin')@section('content')<div class="page-head"><div><span class="eyebrow">HUMAN SUPPORT · CONVERSATION #{{ $conversation->id }}</span><h1>{{ $conversation->user->name }}</h1><p>Declared simulated risk: ${{ number_format($conversation->risk_budget??0,2) }}</p></div><a class="outline" href="{{ route('admin.dashboard') }}">← Queue</a></div><section class="chat glass"><div class="messages">@foreach($conversation->messages as $message)<div class="message {{ $message->author_type==='user'?'user':'assistant' }}"><small>{{ $message->author_type==='human_admin'?'HUMAN ADMIN · '.($message->user?->name??'Support'):strtoupper(str_replace('_',' ',$message->author_type)) }}</small><p>{{ $message->body }}</p></div>@endforeach</div><form class="composer" method="post" action="{{ route('admin.conversations.reply',$conversation) }}">@csrf<textarea name="message" required placeholder="Reply as a clearly identified human administrator…"></textarea><button class="button">Send human reply</button></form></section>@endsection
