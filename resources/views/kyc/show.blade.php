@extends('layouts.app')@section('title','Identity verification — Tesla Markets')@section('content')<div class="page-head"><div><span class="eyebrow">KYC / AML ONBOARDING</span><h1>Verify your identity.</h1><p>Documents are stored on Laravel’s private disk and never served from the public directory.</p></div><span class="status {{ auth()->user()->kyc_status }}">{{ str_replace('_',' ',auth()->user()->kyc_status) }}</span></div><section class="auth-card glass"><form method="post" enctype="multipart/form-data">@csrf<label>Legal name<input name="legal_name" value="{{ old('legal_name',$profile?->legal_name) }}" required></label><label>Date of birth<input type="date" name="date_of_birth" value="{{ old('date_of_birth',$profile?->date_of_birth?->format('Y-m-d')) }}" required></label><label>Country code<input name="country_code" maxlength="2" value="{{ old('country_code',$profile?->country_code) }}" placeholder="US" required></label><label>Document type<select name="document_type"><option value="passport">Passport</option><option value="national_id">National ID</option><option value="drivers_license">Driver’s license</option></select></label><label>Identity document<input type="file" name="document" accept=".pdf,.jpg,.jpeg,.png" required></label><button class="button wide">Submit for review</button></form></section>@endsection
